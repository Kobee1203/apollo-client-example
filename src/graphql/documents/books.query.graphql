query Books($offset: Int, $limit: Int) {
    books(offset: $offset, limit: $limit) {
        id
        title
        author {
            id
            name
            dateOfBirth
            photoUrl
        }
        publicationDate
        coverUrl
    }
}